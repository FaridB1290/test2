import * as React from "react";

import {
  Show,
  SimpleShowLayout,
  ShowProps,
  DateField,
  TextField,
  ReferenceManyField,
  Datagrid,
  ReferenceField,
} from "react-admin";

import { RESTAURANTMANAGEMENT_TITLE_FIELD } from "./RestaurantManagementTitle";

export const RestaurantManagementShow = (
  props: ShowProps
): React.ReactElement => {
  return (
    <Show {...props}>
      <SimpleShowLayout>
        <DateField source="createdAt" label="Created At" />
        <TextField label="ID" source="id" />
        <DateField source="updatedAt" label="Updated At" />
        <ReferenceManyField
          reference="CustomerFeedback"
          target="restaurantManagementId"
          label="CustomerFeedbacks"
        >
          <Datagrid rowClick="show">
            <DateField source="createdAt" label="Created At" />
            <TextField label="ID" source="id" />
            <ReferenceField
              label="RestaurantManagement"
              source="restaurantmanagement.id"
              reference="RestaurantManagement"
            >
              <TextField source={RESTAURANTMANAGEMENT_TITLE_FIELD} />
            </ReferenceField>
            <DateField source="updatedAt" label="Updated At" />
          </Datagrid>
        </ReferenceManyField>
        <ReferenceManyField
          reference="LoyaltyProgram"
          target="restaurantManagementId"
          label="LoyaltyPrograms"
        >
          <Datagrid rowClick="show">
            <DateField source="createdAt" label="Created At" />
            <TextField label="ID" source="id" />
            <ReferenceField
              label="RestaurantManagement"
              source="restaurantmanagement.id"
              reference="RestaurantManagement"
            >
              <TextField source={RESTAURANTMANAGEMENT_TITLE_FIELD} />
            </ReferenceField>
            <DateField source="updatedAt" label="Updated At" />
          </Datagrid>
        </ReferenceManyField>
      </SimpleShowLayout>
    </Show>
  );
};
